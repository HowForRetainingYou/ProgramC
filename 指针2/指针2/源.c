#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>

//指针的运算（前提是在同一块内存空间）：类似于日期的运算，但指针相加没有意义（日期也是一样）
//指针相减的绝对值是两指针间相差的元素个数


//数组名就是元素的首元素地址   数组名==&数组名[0]

//野指针存的是随机值，部分编译器不给访问野指针
//指针非法访问
//尽量不返回局部变量地址

//形式参数有自己的空间
//assert语句
int main()
{

}